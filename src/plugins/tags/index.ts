import type { Tag } from "@roastery/blend";

/** OpenAPI tag metadata for the `Auth` capsule's routes. */
export default {
	name: "Auth",
	description:
		"Authentication and session management, including secure login, rate limiting protection, and access token issuance.",
} as Tag;
