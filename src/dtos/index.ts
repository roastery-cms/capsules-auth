/** Environment-variable contract of the capsule (`AUTH_EMAIL`, `AUTH_PASSWORD`, optional `IGNORE_AUTH`). */
export { AuthEnvDependenciesDTO } from "./auth-env-dependencies.dto";
/** Request-body contract of the `POST /auth/login` endpoint. */
export { VerifyCredentialsDTO } from "./verify-credentials.dto";
